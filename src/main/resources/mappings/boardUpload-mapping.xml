<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="BoardUploadDAO">

	<insert id="addFile">
		INSERT INTO attach (
			FULLNAME,
			BOARDSEQ
		) VALUES (
			#{fileName},
			LAST_INSERT_ID()
		);
	</insert>

</mapper>
